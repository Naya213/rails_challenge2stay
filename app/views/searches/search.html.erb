<h2>Rooms search for "<%= @word %>"</h2>
<h3><%= @rooms.count %>これで検索のヒット数を取得できる</h2>
<table>
  <thead>
    <tr>
      <th>施設名</th>
      <th>住所</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.name %></td>
        <td><%= room.address %></td>
      </tr>
    <% end %>
  </tbody>
</table>